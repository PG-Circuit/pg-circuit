# Mock shop dataset for local pg_circuit demos

Seed + real queries for Community. Captured outputs go to
[`../test-results/mock-demo/`](../test-results/mock-demo/).

```bash
# requires pg_circuit installed for your pg_config
./mock/run.sh
```

Community demos cover enforce blocks for no-WHERE DML / TRUNCATE and status /
runtime / events. Policy, assess, incident, and custom-rule queries are Pro-only.
